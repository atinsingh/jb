{
  "name": "@jobocate/e2e",
  "version": "1.0.0",
  "private": true,
  "description": "Browser end-to-end suite driving the real stack (Next.js + NestJS + MongoDB).",
  "scripts": {
    "test": "playwright test",
    "test:smoke": "playwright test specs/smoke",
    "test:journeys": "playwright test specs/candidate specs/employer specs/auth specs/cross-surface",
    "test:headed": "playwright test --headed",
    "test:ui": "playwright test --ui",
    "report": "playwright show-report",
    "install:browsers": "playwright install chromium"
  },
  "devDependencies": {
    "@playwright/test": "^1.62.1",
    "@types/node": "^20.11.0",
    "typescript": "^5.3.3"
  }
}
