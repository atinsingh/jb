---
alwaysApply: false
description: Optional reuse reminder; Jobocate workflow in AGENTS.md takes precedence
---

Reuse existing API routes, types, and UI modules in `backend/` and `frontend/` before adding new ones. Do not invent Catalyst UI, new logging libraries, or shared `lib/` extracts unless the task asks for that. Graphify first (`graphify.mdc`); do not scan both apps “for completeness” when the change is one-sided.
